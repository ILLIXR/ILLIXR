delimiter: ','

root_path: ...

image:
  # timestamp units aren't mentioned, so the default of nanoseconds
  # will be used.
  - rgb:
      # TUM only has one RGB image stream
      - path: rgb
  - depth:
      # ... and one depth image stream
      - path: depth

ground_truth:
	- pose:
    # datatype isn't mentioned, so the default of `double` will be used.
    # timestamp units aren't mentioned, so the default of nanoseconds
    # will be used.
	  - path: groundtruth.txt
