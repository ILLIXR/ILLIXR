---
- author.astro-friedel
---
This work enables the use of NVENC encoding to offload_rendering_server; allowing encoding directly with the onboard
NVIDIA encoders, without the need of an interface layer (e.g. FFMPEG). This work also brings Windows support to the
offload_rendering_server plugin. The NVENC encoding supports both AV1 and HEVC schemes.
