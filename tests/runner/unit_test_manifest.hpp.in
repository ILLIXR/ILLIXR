#pragma once

// Generated by CMake from UNIT_TEST_LIST at configure time (see the BUILD_UNIT_TESTS branch of
// src/CMakeLists.txt's Android build). Do not edit directly -- every entry here was already a
// link-time dependency of unit-tests-activity, so it's already resident in the process; see
// discover_candidates() in unit_test_runner.cpp.

#include <array>
#include <string_view>

namespace ILLIXR {

inline constexpr std::array<std::string_view, @ILLIXR_UNIT_TEST_COUNT@> k_unit_test_libraries = {
@UNIT_TEST_MANIFEST_ENTRIES@};

} // namespace ILLIXR
